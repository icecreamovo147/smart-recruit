# Knowledge Archive

Archive preserves superseded context that still explains historical behavior.

- Archived files use `status: archived` or `status: deprecated` before final movement.
- Archive is excluded from default Agent retrieval.
- Historical content must not be silently restored as an active implementation source.
- Prefer an active replacement document or ADR link when available.
