# TASK-012 Acceptance - 增强语义召回调试页 Provider 状态展示

## 1. 任务目标

在语义召回调试页面展示 provider/model、维度、候选数量、query embedding 耗时和降级原因。

## 2. 自动化测试检查项

- 执行 `pnpm --filter hr-frontend typecheck`，结果必须通过；若项目暂未配置该命令，必须在报告中说明。
- 执行 `pnpm --filter hr-frontend test`，结果必须通过；若项目暂未配置该命令，必须在报告中说明。
- 执行 `pnpm --filter hr-frontend build`，结果必须通过；若项目暂未配置该命令，必须在报告中说明。
- 验证：未运行状态展示占位。
- 验证：provider 可用展示 provider/model/维度/耗时。
- 验证：fallback 展示原因且页面不报错。
- 验证：typecheck 通过。

## 3. 手工验收检查项

- 对照 TASKS.md 确认修改文件未超出允许范围。
- 对照 task-scope.json 检查 git diff 中的文件是否越界。
- 确认实现符合 SPEC/SDD，不引入非目标范围能力。
- 确认日志、错误返回和测试数据没有泄漏 API Key 或完整敏感文本。
- 在 1366px、1440px、1920px 宽度下检查语义召回调试页布局。

## 4. 异常场景检查项

- Provider 未配置或不可用时，不应阻塞主业务流程。
- 外部接口失败时，错误必须可观测且脱敏。
- 输入为空、配置缺失、权限不足或数据不存在时，应返回明确错误或进入降级状态。

## 5. 回归测试范围

- 语义召回调试页。
- Agent Skill 管理页不应变化。

## 6. 完成后必须输出的报告内容

- TASK-012 是否完成，以及完成摘要。
- 实际修改文件列表。
- 执行过的命令、结果和失败原因。
- 未执行的命令及原因。
- 是否触碰公共模块或越界文件。
- 风险点是否已处理。
- 回滚方式。
- 后续建议执行的 TASK。
