# CR-0007 — Application-analysis message validity

Runtime evidence shows the HR route-entry application-analysis flow creates a session without a user message, then starts a durable Run with `message=""`. The model message builder consequently produces System-only input, and the Anthropic adapter serializes its nil conversation slice as `messages:null`, which the provider rejects with HTTP 400.

This amendment adds one vertical TASK to seed and return a canonical candidate-match request, keep both HR frontend analysis entries compatible, reject blank durable Run input at HTTP and gRPC boundaries, and reject System-only Anthropic requests locally. It uses existing protobuf fields and storage methods and introduces no schema, Proto, dependency, lockfile, configuration, authorization, or privacy expansion.

The HTTP/gRPC validation semantics and shared AI adapter write make this L2. The user's instruction “把这个问题解决一下” follows the diagnosis that explicitly proposed these three defenses and is recorded as approval for this bounded repair. Independent amendment and implementation Review remain mandatory.
