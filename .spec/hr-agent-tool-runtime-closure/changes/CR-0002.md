# CR-0002 — TASK-002 deterministic formatter scope correction

TASK-002 correctly routes factual live-data intents to deterministic rendering after successful Tool evidence. During acceptance coverage, `list_all_applications` returned one real application while `formatApplicationList` read the obsolete `candidates` field and rendered an empty result. Candidate search also needs a formatter-aligned keyword extraction test.

This L0 correction adds only `smart-recruit-commons/ai/fallback.go` and matching tests to TASK-002 scope so the already-required grounded response can represent the Tool contract accurately. It does not change requirements, architecture, public contracts, schema, dependencies, security policy, or confirmation behavior.
