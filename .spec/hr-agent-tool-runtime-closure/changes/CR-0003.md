# CR-0003 — TASK-002 knowledge scope correction

The required impact detector reports `update_required`. TASK-002 adds deterministic per-intent evidence groups, blocks model generation when any required group is missing or failed, renders direct factual queries from Tool results, and propagates clamped Agent `max_iterations` into the shared Tool loop. The active Agent runtime and AI configuration governance documents currently stop at TASK-001 fail-closed semantics.

This L0 correction adds only those two routed active documents to TASK-002 write scope. It changes no requirement, behavior, API, schema, dependency, or security policy; TASK-005 retains cumulative final knowledge validation.
