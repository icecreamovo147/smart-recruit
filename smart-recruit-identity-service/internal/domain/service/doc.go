// Package service contains domain services for Identity rules that span models.
//
// Domain services must remain deterministic and should not perform database,
// Redis, gRPC, HTTP, Nacos, or environment access.
package service
