// Package persistence will hold Identity GORM repository implementations.
//
// Implementations in this package may access Identity-owned tables only, unless
// a later TASK explicitly records a permitted transitional read contract.
package persistence
