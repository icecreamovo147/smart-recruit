apiVersion: apps/v1
kind: Deployment
metadata:
  name: logic-worker
  namespace: recruitment
  labels:
    app: logic-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: logic-worker
  template:
    metadata:
      labels:
        app: logic-worker
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: worker
          image: recruitment/logic-grpc-service:latest
          command:
            - /usr/local/bin/logic-grpc-service
            - --worker-only
          ports:
            - containerPort: 9091
              name: metrics
            - containerPort: 9092
              name: health
          envFrom:
            - configMapRef:
                name: logic-config
            - secretRef:
                name: recruitment-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /livez
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: worker-pdb
  namespace: recruitment
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: logic-worker
