# Nacos Seed Config

These files are non-sensitive local examples for Nacos Config. They intentionally avoid tokens, passwords, private keys, provider credentials, and real `.env` values.

Seed them manually during local testing after Nacos starts, or use a later deployment task's seed script when it is introduced.
