package domain

// Package domain owns AI Agent business concepts, invariants, policies, events,
// and repository ports. It must not depend on GORM, Redis, RabbitMQ, gRPC, AI
// provider SDKs, MCP SDKs, configuration loaders, or environment variables.
