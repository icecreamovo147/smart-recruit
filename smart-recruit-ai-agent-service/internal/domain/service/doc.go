package service

// Package service will contain pure domain services for AI Agent invariants,
// prompt selection rules, provider fallback decisions, MCP policy checks, and
// agent-run lifecycle transitions.
