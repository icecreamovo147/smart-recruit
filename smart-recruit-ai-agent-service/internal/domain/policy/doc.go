package policy

// Package policy owns deterministic AI Agent rules such as provider failover
// eligibility, MCP allowlist checks, skill selection limits, stream timeout
// limits, and sensitive-data redaction boundaries.
