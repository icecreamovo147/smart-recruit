package infrastructure

// Package infrastructure contains concrete AI Agent adapters for persistence,
// cache, message queues, AI providers, embedding providers, MCP clients,
// encryption, and cross-service clients.
