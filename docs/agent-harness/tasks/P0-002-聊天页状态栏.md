# 任务编号：P0-002

## 1. 任务目标

在 HR 端聊天页顶部增加一行状态栏，展示：
- 当前使用的模型名称
- 数据来源标识（如「数据来源：业务数据库」）
- 后续可扩展显示对话中的工具调用提示

## 2. 对应缺陷

- **缺陷 13**：当前 Agent / 模型 / 数据源状态不明确

## 3. 前置条件

- P0-001 完成（可选，非硬依赖）

## 4. 允许修改的文件

- `hr-frontend/src/views/hr/AIChatView.vue`
- `hr-frontend/src/types/ai.ts`（如需要新增类型）

## 5. 禁止修改的文件

- `logic-grpc-service/` 下所有文件
- `web-gin-service/` 下所有文件
- 其他前端文件

## 6. 实现步骤

1. 在 `AIChatView.vue` 消息区域顶部新增状态栏组件（会话信息区域与消息列表之间）
2. 展示当前模型名称（从 SSE 流事件中可获取，暂时可硬编码为配置文件中的模型名，等 M2 阶段改动态）
3. 展示数据来源标识：固定为「数据来源：招聘业务数据库」
4. 状态栏样式：使用 Element Plus `el-alert` 或 `el-tag` 内联样式，`type="info"` 浅色背景
5. 状态栏可折叠（默认展开，提供收起图标）

## 7. 数据库变更

不涉及

## 8. 接口变更

不涉及（本阶段模型名先硬编码读取，M2 阶段改为动态接口）

## 9. 前端变更

- `AIChatView.vue`：新增状态栏行
- 状态栏包含：模型名标签、数据来源标签

## 10. 权限与安全要求

- 无新增权限

## 11. 验收标准

- [ ] 聊天页顶部显示一行状态信息（模型名 + 数据来源）
- [ ] 状态信息可折叠/展开
- [ ] 状态栏不与已有 UI 冲突
- [ ] 已有多会话切换、创建、重命名功能不受影响
- [ ] `pnpm typecheck` 通过
- [ ] `pnpm build` 通过

## 12. 必须运行的测试命令

```bash
cd hr-frontend && pnpm install && pnpm typecheck && pnpm build
```

## 13. 完成后必须输出的内容

- 变更摘要
- 构建验证结果
- UI 截图（如可启动 dev server）

## 14. 回滚方案

恢复 `AIChatView.vue` 修改前版本。

---

## 完成记录

**完成日期**：2026-06-26
**开发者**：task-developer (Agent)

### 变更文件清单
- `hr-frontend/src/views/hr/AIChatView.vue` - 在会话信息区域与消息列表之间新增状态栏组件，包含模型名标签和数据来源标签，支持折叠/展开

### 验证结果
- [x] `pnpm install` - PASS
- [x] `pnpm typecheck` (vue-tsc --noEmit) - PASS (零类型错误)
- [x] `pnpm build` (vite build) - PASS (零构建错误)
- [x] 仅修改允许的文件 (`hr-frontend/src/views/hr/AIChatView.vue`)
- [x] 无 TODO/FIXME/HACK 残留
- [x] 无调试代码 (console.log)
- [x] 未修改禁止的文件

### 验收标准检查
- [x] 聊天页顶部显示一行状态信息（模型名 + 数据来源）
- [x] 状态信息可折叠/展开
- [x] 状态栏不与已有 UI 冲突
- [x] 已有多会话切换、创建、重命名功能不受影响
- [x] `pnpm typecheck` 通过
- [x] `pnpm build` 通过
